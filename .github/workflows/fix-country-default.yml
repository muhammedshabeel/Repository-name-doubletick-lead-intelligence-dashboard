name: Apply campaign-only lead mapping

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/fix-country-default.yml"

permissions:
  contents: write

jobs:
  apply-mapping-fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - name: Update campaign mapping logic
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path

          path = Path("app.py")
          text = path.read_text